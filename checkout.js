let cart=JSON.parse(localStorage.getItem('ba_cart')||'[]');const $=s=>document.querySelector(s);const esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
function save(){localStorage.setItem('ba_cart',JSON.stringify(cart))}function total(){return cart.reduce((a,x)=>a+Number(x.price)*x.qty,0)}
function render(){const box=$('#cartBox');if(!cart.length){box.innerHTML='<div class="cart-list empty">السلة فارغة.<br><br><a class="primary" href="index.html">العودة للمنتجات</a></div>';$('#total').textContent='0.00 درهم';return}box.innerHTML='<div class="cart-list">'+cart.map((x,i)=>`<div class="cart-item"><div class="mini-photo">${x.image?`<img src="${esc(x.image)}" style="width:100%;height:100%;object-fit:cover;border-radius:13px">`:'✦'}</div><div><h3>${esc(x.name)}</h3><small>${Number(x.price).toFixed(2)} درهم — ${esc(x.cat||'')}</small><br><button class="remove" data-remove="${i}">حذف</button></div><div class="qty"><button data-minus="${i}">−</button><b>${x.qty}</b><button data-plus="${i}">+</button></div></div>`).join('')+'</div>';$('#total').textContent=total().toFixed(2)+' درهم'}
document.addEventListener('click',e=>{let b=e.target.closest('[data-plus],[data-minus],[data-remove]');if(!b)return;let i=Number(b.dataset.plus??b.dataset.minus??b.dataset.remove);if(b.dataset.plus!==undefined)cart[i].qty++;if(b.dataset.minus!==undefined){cart[i].qty--;if(cart[i].qty<=0)cart.splice(i,1)}if(b.dataset.remove!==undefined)cart.splice(i,1);save();render()});
$('#submit').onclick=()=>{const phone=$('#phone').value.trim(),msg=$('#msg');if(!cart.length){msg.hidden=false;msg.textContent='السلة فارغة.';return}if(!phone){msg.hidden=false;msg.textContent='يرجى إدخال رقم الهاتف للتواصل معك.';return}let orders=JSON.parse(localStorage.getItem('ba_orders')||'[]');orders.unshift({id:'BA-'+Date.now(),createdAt:new Date().toISOString(),name:$('#name').value.trim()||'بدون اسم',phone,city:$('#city').value.trim(),notes:$('#notes').value.trim(),items:cart,total:total(),status:'new'});localStorage.setItem('ba_orders',JSON.stringify(orders));cart=[];save();$('#cartBox').innerHTML='<div class="success"><h2>تم استلام طلبك ✅</h2><p>مهم: سنتواصل معك قريبًا على رقم الهاتف الذي أدخلته لتأكيد الطلبية.</p><a class="primary" href="index.html">العودة للتسوق</a></div>';$('#total').textContent='0.00 درهم';$('#submit').disabled=true};render();
